import { CurrenyDirectiveDirective } from './curreny-directive.directive';

describe('CurrenyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrenyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
